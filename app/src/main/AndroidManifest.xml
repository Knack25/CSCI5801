<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wfd">
    <!-- Declaring permissions to be used -->
    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WFD">
        <activity
            android:name=".recipe.RecipeDetails"
            android:exported="false" />
        <activity
            android:name=".recipe.RecipeAddIngredients"
            android:exported="false" />
        <activity
            android:name=".recipe.Add_Recipe_Popup"
            android:exported="false" />
        <activity
            android:name=".recipe.RecipeActivity"
            android:exported="false" />
        <activity
            android:name=".pantry.Add_Ingredient_Popup"
            android:exported="false" />
        <activity android:name=".Shopping" />
        <activity android:name=".pantry.Pantry" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WFD.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CameraActivity" />
    </application>

</manifest>